# CLAUDE.md — handoff-admin

このフォルダは SalonRink 管理画面（ログイン後）のニューモーフィズム再デザインの引き継ぎパッケージです。

- まず `実装指示書.md` を読むこと。
- `SalonRink 管理画面 Neo.html` + `admin-neo.css` が確定デザインの実物。実装の見た目は必ずこれに合わせる。
- HTML側のクラス名・CSS変数はそのまま流用してよい（フレームワークに合わせたリネームは可、見た目の変更は不可）。
- 判断に迷う点（データ結線・既存ルーティングとの整合など）は推測で進めず、TODOコメントを残して質問にまとめること。
